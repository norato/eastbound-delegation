<%= render partial: "twitter_tags", locals: { title: "Adopt this Bernie Delegate from #{params[:state]}", description: "Click Here! #{pretty_name(@delegate)} will be OUR voice at the Democratic Convention, let's make sure they get there. Donate & share!" } %>
<%= render partial: "show", locals: { delegate: @delegate } %>
